# Product Listing Webpage

A clean and responsive product listing web application built with **Next.js** and **React**. This project demonstrates fetching products from a dummy API, filtering, searching, infinite scrolling dark/light mode and sorting.

---

## 🛠 Tools & Technologies Used

- **Next.js** – React framework for server-side rendering and static site generation
- **React** – Frontend UI library
- **TypeScript** – Type-safe JavaScript
- **TailwindCSS** – Utility-first CSS framework
- **shadcn UI** – Accessible UI components
- **React Query (@tanstack/react-query)** – Data fetching and caching
- **Zustand** – Lightweight state management

---

## ⚡ Project Setup

### 1. Clone the repository

```bash
git clone https://github.com/novanish/task
cd task
```

### 2. Install dependencies

```bash
pnpm install
```

### 3. Create a `.env` file and set NEXT_PUBLIC_API_BASE_URL=https://fakeapi.in/api or copy from .env.example

### 4. Build the project

```bash
pnpm build
```

### 5. Start preview server

```bash
pnpm start
```
